# malice/rizin plugin configuration.

name = 'rizin'
description = 'rizin (rz-bin) binary analysis: headers, sections, imports, exports, symbols, strings'
category = 'exe'
version = '0.1.0'

accepts = [ '*' ]

[system]
cpu = 0.5
ram = 256 # MB

[settings]
